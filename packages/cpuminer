#!/bin/bash

depends_on curl
depends_on openssl

fetch_source https://github.com/nicehash/cpuminer-multi/archive/windows.tar.gz

unset EXTRAFLAGS
cc_addflags EXTRAFLAGS -Ofast -flto -fuse-linker-plugin -ftree-loop-if-convert-stores -pg -DUSE_ASM

do_unpack
do_autoreconf
CPPFLAGS="$CPPFLAGS $EXTRAFLAGS" \
do_compile --with-crypto --with-curl
