#!/bin/bash

needs_tool perl

fetch_source https://exiftool.org/Image-ExifTool-12.30.tar.gz c873d35775921658585d330f846c3cd9fbc1b59b

do_unpack
pushd_src
perl Makefile.PL INSTALL_BASE="$PREFIX"
sed -i.bak "s|\$exeDir/lib|$PREFIX/lib/perl5|" exiftool
make install
popd_src
