#!/usr/bin/bash

fetch_source https://ftpmirror.gnu.org/sed/sed-4.10.tar.xz 15f5d702ae4360c3e6a79c48984830aa55396855

CONFFLAGS=(
--program-prefix=g
--disable-nls      ## otherwise will try to link with gettext's libintl
)

do_unpack_compile "${CONFFLAGS[@]}"
