#!/bin/bash

[[ $OSTYPE != *linux* ]] && log "libmd is a linux-specific library, refusing to build" && false

fetch_source https://archive.hadrons.org/software/libmd/libmd-1.1.0.tar.xz 72718d5ffad112b702ba84fd40900486f8179642

do_unpack_compile
