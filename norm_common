#!/usr/bin/env bash

# sourced by norm

TARDIR="$NORMDIR/archives"

LIBSUFFIX=so
[[ $OSTYPE == *darwin* ]] && LIBSUFFIX=dylib

. "$NORMDIR"/norm_common.functions

set -eE
set -o pipefail
trap onerror ERR
trap onexit EXIT

export COMPILEDIR="$HOME/tmp/compile"

